# HF系列激光测距传感器 MODBUS协议文档

> 基于HF系列激光测距传感器产品手册整理  
> 通信协议：MODBUS-RTU over RS485

---

## 目录

1. [通信规格](#1-通信规格)
2. [菜单设置与操作](#2-菜单设置与操作)
3. [MODBUS寄存器定义](#3-modbus寄存器定义)
4. [通信示例](#4-通信示例)
5. [CRC校验说明](#5-crc校验说明)

---

## 1. 通信规格

| 参数 | 规格 |
|------|------|
| 通信协议 | MODBUS-RTU |
| 通信接口 | RS485 |
| 通信方式 | 半双工 |
| 波特率 | 9600 / 19200 / 38400 / 57600 / 115200 / 230400 bps |
| 数据位 | 8位 |
| 停止位 | 1位 |
| 奇偶校验 | 无 |
| 默认从机地址 | 1 |
| 默认波特率 | 9600 |

### 支持的功能码

| 功能码 | 名称 | 说明 |
|--------|------|------|
| `0x04` | 读取输入寄存器 | 读取测量距离值等数据 |
| `0x03` | 读取保持寄存器 | 读取设备参数 |
| `0x06` | 预置单寄存器 | 写入单个参数 |
| `0x10` | 预置多寄存器 | 写入多个参数 |

---

## 2. 菜单设置与操作

### 2.1 进入菜单

在正常显示界面下**长按2秒"M"键**，进入菜单设置模式。

- 在菜单设置模式下，**长按2秒"M"键**，退出菜单设置模式
- 在菜单设置模式下，**停止20秒不按任何按键**，自动退出菜单设置模式
- 进入菜单后，按**"UP"/"DOWN"键**上下切换菜单项
- 按**"SET"键**进入对应菜单项

### 2.2 菜单功能列表

| 序号 | 菜单项 | 说明 | 可选值 |
|------|--------|------|--------|
| 1 | **工作模式** | 测距模式/标准 | 测距模式、标准 |
| 2 | **常开常闭** | 开关量输出逻辑 | 常开、常闭 |
| 3 | **检测输出** | 教导检测模式 | 通常模式、一点教导、二点教导、三点教导 |
| 4 | **模拟选择** | 模拟量输出类型 | 0-5V、4-20mA |
| 5 | **迟滞** | 开关输出迟滞量 | 0~65535 |
| 6 | **外部输入** | 外部触发模式 | 关闭、触发（颜色按钮按电源负载一次>30ms为触发一次） |
| 7 | **输出定时** | 输出定时模式 | 无定时、输出延长、延迟输出、单次输出、时间设置（默认5ms） |
| 8 | **显示模式** | 数值显示方式 | 标准（实际距离）、反转（量程中心为0点）、偏移（量程前段为0点） |
| 9 | **保持** | 检测值保持功能 | 保持关、保持开（当前检测值在最大/最小值时，输出电压/电流保持） |
| 10 | **显示屏选择** | 屏幕背光控制 | 定时黑屏、常亮 |
| 11 | **恢复出厂设置** | 恢复出厂参数 | 长按10秒确认（仅在此菜单显示） |
| 12 | **波特率设置** | 通信波特率 | 9600/19200/38400/57600/115200/230400 |
| 13 | **通讯地址** | MODBUS从机地址 | 1~247，默认1 |
| 14 | **零点校正** | 距离偏差修正 | 可手动设置距离偏差，修正整体误差 |
| 15 | **复位** | 确认恢复出厂 | 选中确认复位后，显示"已恢复出厂设置"则成功 |
| 16 | **语言** | 显示语言 | 简体中文、English |

### 2.3 按键操作

#### 调零功能
> **注意**：调零需在显示模式设置为**反转**或**偏移模式**下方可操作。

调零功能是指测量值强制"置零"的功能。

- **设置调零**：按"SET"键进入设定，再按"UP"键；同时按下"M"键和"UP"键，设置调零
- **取消调零**：同时按下"M"键和"DOWN"键，调零取消

#### 按键锁定

- **锁定按键**：同时按下"M"键和"DOWN"键
- **按键解锁**：同时按下"M"键和"UP"键

---

## 3. MODBUS寄存器定义

### 3.1 输入寄存器（功能码 0x04）

| 寄存器地址 | 名称 | 数据类型 | 说明 | 单位 |
|------------|------|----------|------|------|
| `0x0000` | **距离值高位** | uint16 | 测量距离值高16位 | 0.1mm |
| `0x0001` | **距离值低位** | uint16 | 测量距离值低16位 | 0.1mm |

> **距离值说明**：距离值为32位无符号整数（`0x0000`为高16位，`0x0001`为低16位），实际距离 = 寄存器值 × 0.1mm。
> 
> 例如：距离值 = `0x00001916` = 6422，即实际距离 = **642.2mm**。

### 3.2 保持寄存器（功能码 0x03 / 0x06 / 0x10）

| 寄存器地址 | 名称 | 数据类型 | 读写权限 | 范围 | 默认值 | 说明 |
|------------|------|----------|----------|------|--------|------|
| `0x0002` | **波特率** | uint16 | R/W | 0~5 | 0 | 通信波特率设置 |
| `0x0003` | **从机地址** | uint16 | R/W | 1~247 | 1 | MODBUS从机地址 |
| `0x0004` | **工作模式** | uint16 | R/W | 0~1 | 0 | 0=测距模式，1=标准 |
| `0x0005` | **输出逻辑** | uint16 | R/W | 0~1 | 0 | 0=常开，1=常闭 |
| `0x0006` | **教导模式** | uint16 | R/W | 0~3 | 0 | 0=通常，1=一点，2=二点，3=三点 |
| `0x0007` | **模拟输出类型** | uint16 | R/W | 0~1 | 0 | 0=0-5V，1=4-20mA |
| `0x0008` | **迟滞值** | uint16 | R/W | 0~65535 | 0 | 开关输出迟滞量 |
| `0x0009` | **外部输入模式** | uint16 | R/W | 0~1 | 0 | 0=关闭，1=触发 |
| `0x000A` | **输出定时** | uint16 | R/W | 0~4 | 0 | 输出定时模式 |
| `0x000B` | **显示模式** | uint16 | R/W | 0~2 | 0 | 0=标准，1=反转，2=偏移 |
| `0x000C` | **保持功能** | uint16 | R/W | 0~1 | 0 | 0=关闭，1=开启 |
| `0x000D` | **显示屏** | uint16 | R/W | 0~1 | 0 | 0=定时黑屏，1=常亮 |
| `0x000E` | **恢复出厂设置** | uint16 | W | 0~1 | 0 | 写1恢复出厂设置 |
| `0x000F` | **零点偏移** | uint16 | R/W | - | 0 | 距离修正值（单位0.1mm） |
| `0x0010` | **语言设置** | uint16 | R/W | 0~1 | 0 | 0=简体中文，1=English |

### 3.3 参数详细说明

#### 波特率（地址 0x0002）

| 寄存器值 | 波特率 |
|----------|--------|
| 0 | 9600 bps（默认） |
| 1 | 19200 bps |
| 2 | 38400 bps |
| 3 | 57600 bps |
| 4 | 115200 bps |
| 5 | 230400 bps |

> ⚠️ **注意**：修改波特率后需**断电重启**方可生效。

#### 工作模式（地址 0x0004）

| 寄存器值 | 模式 |
|----------|------|
| 0 | 测距模式 |
| 1 | 标准 |

#### 输出逻辑（地址 0x0005）

| 寄存器值 | 模式 |
|----------|------|
| 0 | 常开（NO） |
| 1 | 常闭（NC） |

#### 教导模式（地址 0x0006）

| 寄存器值 | 模式 |
|----------|------|
| 0 | 通常模式 |
| 1 | 一点教导 |
| 2 | 二点教导 |
| 3 | 三点教导 |

#### 模拟输出类型（地址 0x0007）

| 寄存器值 | 输出类型 |
|----------|----------|
| 0 | 0-5V |
| 1 | 4-20mA |

#### 外部输入模式（地址 0x0009）

| 寄存器值 | 模式 |
|----------|------|
| 0 | 关闭 |
| 1 | 触发（颜色按钮按电源负载一次>30ms为触发一次） |

#### 输出定时（地址 0x000A）

| 寄存器值 | 模式 |
|----------|------|
| 0 | 无定时 |
| 1 | 输出延长 |
| 2 | 延迟输出 |
| 3 | 单次输出 |
| 4 | 时间设置（默认5ms，菜单可调） |

#### 显示模式（地址 0x000B）

| 寄存器值 | 模式 | 说明 |
|----------|------|------|
| 0 | 标准 | 显示实际距离 |
| 1 | 反转 | 量程中心点为0点，靠近传感器方向为负 |
| 2 | 偏移 | 量程前段变为0点，靠近传感器方向为负偏移 |

#### 保持功能（地址 0x000C）

| 寄存器值 | 模式 |
|----------|------|
| 0 | 保持关 |
| 1 | 保持开 |

> 保持功能开启后，当检测值达到最大值或最小值时，输出电压/电流将保持该值。应用场景：超出量程后仍保持0V或5V输出。

#### 显示屏（地址 0x000D）

| 寄存器值 | 模式 |
|----------|------|
| 0 | 定时黑屏 |
| 1 | 常亮 |

#### 语言设置（地址 0x0010）

| 寄存器值 | 语言 |
|----------|------|
| 0 | 简体中文 |
| 1 | English |

---

## 4. 通信示例

### 4.1 读取距离值（功能码 0x04）

**请求帧（主机 → 传感器）**：

| 字段 | 值 | 说明 |
|------|-----|------|
| 从机地址 | `0x01` | 设备地址1 |
| 功能码 | `0x04` | 读取输入寄存器 |
| 起始地址高字节 | `0x00` | 寄存器地址0x0000 |
| 起始地址低字节 | `0x00` | |
| 寄存器数量高字节 | `0x00` | 读取2个寄存器 |
| 寄存器数量低字节 | `0x02` | |
| CRC低字节 | `0x71` | CRC校验 |
| CRC高字节 | `0xCB` | |

**完整请求帧**：
```
01 04 00 00 00 02 71 CB
```

**响应帧（传感器 → 主机）**：

| 字段 | 值 | 说明 |
|------|-----|------|
| 从机地址 | `0x01` | 设备地址1 |
| 功能码 | `0x04` | 读取输入寄存器 |
| 字节数 | `0x04` | 4字节数据 |
| 数据1（高字节） | `0x00` | 距离值高16位高位 |
| 数据2（低字节） | `0x00` | 距离值高16位低位 |
| 数据3（高字节） | `0x19` | 距离值低16位高位 |
| 数据4（低字节） | `0x16` | 距离值低16位低位 |
| CRC低字节 | `0x21` | CRC校验 |
| CRC高字节 | `0xC2` | |

**完整响应帧**：
```
01 04 04 00 00 19 16 21 C2
```

**数据解析**：
- 距离值 = `0x00001916` = **6422**
- 实际距离 = 6422 × 0.1mm = **642.2mm**

---

### 4.2 设置波特率为9600（功能码 0x10）

**请求帧（主机 → 传感器）**：

| 字段 | 值 | 说明 |
|------|-----|------|
| 从机地址 | `0x01` | 设备地址1 |
| 功能码 | `0x10` | 预置多寄存器 |
| 起始地址高字节 | `0x00` | 寄存器地址0x0002 |
| 起始地址低字节 | `0x02` | |
| 寄存器数量高字节 | `0x00` | 写1个寄存器 |
| 寄存器数量低字节 | `0x01` | |
| 字节数 | `0x02` | 2字节数据 |
| 数据1（高字节） | `0x00` | 波特率值高位 |
| 数据2（低字节） | `0x00` | 波特率值低位（0=9600） |
| CRC低字节 | `0xA9` | CRC校验 |
| CRC高字节 | `0x13` | |

**完整请求帧**：
```
01 10 00 02 00 01 02 00 00 A9 13
```

**响应帧（传感器 → 主机）**：

| 字段 | 值 | 说明 |
|------|-----|------|
| 从机地址 | `0x01` | 设备地址1 |
| 功能码 | `0x10` | 预置多寄存器 |
| 起始地址高字节 | `0x00` | 寄存器地址0x0002 |
| 起始地址低字节 | `0x02` | |
| 寄存器数量高字节 | `0x00` | 写1个寄存器 |
| 寄存器数量低字节 | `0x01` | |
| CRC低字节 | `0xA9` | CRC校验 |
| CRC高字节 | `0xCB` | |

**完整响应帧**：
```
01 10 00 02 00 01 A9 CB
```

> ⚠️ **注意**：修改波特率后需**断电重启**方可生效。

---

### 4.3 读取单个保持寄存器（功能码 0x03）

以读取从机地址（地址0x0003）为例：

**请求帧**：
```
01 03 00 03 00 01 74 0A
```

**响应帧**（假设从机地址为1）：
```
01 03 02 00 01 79 84
```

---

### 4.4 写入单个保持寄存器（功能码 0x06）

以设置从机地址为2（地址0x0003）为例：

**请求帧**：
```
01 06 00 03 00 02 39 CB
```

**响应帧**：
```
01 06 00 03 00 02 39 CB
```

---

## 5. CRC校验说明

MODBUS-RTU采用**CRC-16**校验，多项式为：

```
G(x) = x^16 + x^15 + x^2 + 1
```

即多项式值：`0x8005`

### CRC计算步骤

1. 预置一个16位寄存器为`0xFFFF`
2. 把数据帧中的第一个字节的8位与CRC寄存器中的低字节进行异或运算
3. 把CRC寄存器向右移一位，最高位填0
4. 如果移出的位为1，则将CRC寄存器与`0xA001`进行异或
5. 重复步骤3和4共8次，完成一个字节的CRC计算
6. 对数据帧的下一个字节重复步骤2~5
7. 最后得到的CRC寄存器值即为CRC校验值
8. **CRC低字节在前，高字节在后**附加在数据帧末尾

### Python CRC计算示例

```python
def modbus_crc(data: bytes) -> bytes:
    """计算MODBUS-RTU CRC-16校验"""
    crc = 0xFFFF
    for byte in data:
        crc ^= byte
        for _ in range(8):
            if crc & 0x0001:
                crc = (crc >> 1) ^ 0xA001
            else:
                crc >>= 1
    return bytes([crc & 0xFF, (crc >> 8) & 0xFF])

# 示例：计算读取距离请求的CRC
request = bytes([0x01, 0x04, 0x00, 0x00, 0x00, 0x02])
crc = modbus_crc(request)
print(f"CRC: {crc.hex().upper()}")  # 输出: 71CB
```

---

## 附录：快速参考

### 常用命令速查

| 功能 | 命令帧（HEX） |
|------|--------------|
| 读取距离值 | `01 04 00 00 00 02 71 CB` |
| 读取波特率 | `01 03 00 02 00 01 25 CA` |
| 设置波特率9600 | `01 10 00 02 00 01 02 00 00 A9 13` |
| 读取从机地址 | `01 03 00 03 00 01 74 0A` |
| 设置从机地址为2 | `01 06 00 03 00 02 39 CB` |
| 恢复出厂设置 | `01 06 00 0E 00 01 E9 C9` |

### 波特率修改流程

```
1. 发送修改波特率命令（功能码0x10，地址0x0002）
2. 等待传感器正确响应
3. 断电重启传感器
4. 主机切换至新波特率进行通信
```

---

> **文档版本**: V1.0  
> **整理日期**: 2026-08-25  
> **适用型号**: HF系列激光测距传感器  
> **通信协议**: MODBUS-RTU
